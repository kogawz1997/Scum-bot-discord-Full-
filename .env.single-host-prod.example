# Single-host production profile
# Use when one Windows/Linux host runs:
# - bot (Discord + admin web + delivery/rentbike workers)
# - watcher
# - standalone player portal
# This profile stays PostgreSQL-first and keeps owner/tenant/player split in the UI.

NODE_ENV=production
DATABASE_PROVIDER=postgresql
PRISMA_SCHEMA_PROVIDER=postgresql
DATABASE_URL="postgresql://app:change-me@127.0.0.1:5432/scum_th_platform?schema=public"
PERSIST_REQUIRE_DB=true
PERSIST_LEGACY_SNAPSHOTS=false
TENANT_DB_ISOLATION_MODE=postgres-rls-strict
TENANT_DB_TOPOLOGY_MODE=schema-per-tenant

ADMIN_WEB_SESSION_COOKIE_PATH=/
ADMIN_WEB_SESSION_COOKIE_DOMAIN=admin.example.com
ADMIN_WEB_SECURE_COOKIE=true
ADMIN_WEB_ALLOWED_ORIGINS=https://admin.example.com

BOT_ENABLE_ADMIN_WEB=true
BOT_ENABLE_RENTBIKE_SERVICE=true
BOT_ENABLE_DELIVERY_WORKER=true
BOT_HEALTH_PORT=3210

WORKER_ENABLE_RENTBIKE=false
WORKER_ENABLE_DELIVERY=false
WORKER_HEALTH_PORT=0

SCUM_WATCHER_HEALTH_PORT=3212
